{% include 'default/_form.html.twig' with {
    page_title: 'title.film_new',
    attr: {
        method: 'POST',
        action: url('film_new')
    },
    form: form,
    submit_label: 'action_create'|trans,
    back_to_list_path: 'films_index',
} %}